/*
 **Purpose: Controller currently does not define any needed data or functionality.
 */
define(['angular', 'Logger', './module'], function (ng, Logger, controllersModule) {

    'use strict';

    Logger.debug('Registering RemotePersonConnectionListController controller.');

    controllersModule.controller('RemotePersonConnectionListController', ['$scope', function ($scope) {

    }]);
});