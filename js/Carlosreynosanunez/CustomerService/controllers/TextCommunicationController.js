/*
 **Purpose: Currently no functionality is needed.
 */
define(['angular', 'Logger', './module'], function (ng, Logger, controllersModule) {

    'use strict';

    Logger.debug('Registering controller TextCommunicationController');

    controllersModule.controller('TextCommunicationController', ['$scope', function ($scope) {

    }]);
});